export class CambioEstadoPedido {
    constructor(fecha, estado, usuario, motivo) {
        this.fecha = fecha
        this.estado = estado
        this.usuario = usuario
        this.motivo = motivo
    }
}
